# تقرير قبول إصلاح CORVAX RC27.4 R9.1 العاجل

التاريخ: 2026-08-10  
الإصدار: `1.0.0-agreement-completion-rc27.4-r9.1`  
رأس الهجرات: `e20500000001` — لم تُضف هجرة قاعدة بيانات جديدة.

## النتيجة التنفيذية

تم إغلاق المشكلتين المطلوبتين:

1. تفعيل مسح شامل وآمن لبيانات التشغيل في بيئة UAT حتى يمكن إدخال بيانات اختبار شبه حقيقية.
2. إصلاح إنشاء المستخدم عندما يحتوي الاسم أو اسم الدخول على مسافات، مثل `Hussein Mahmoud`.

## نطاق مسح UAT

المسح يشمل جميع الشركات الموجودة في النظام وليس الشركة المحددة فقط. يحذف محتوى 279 جدول تشغيل، ومنها الأطراف والأصناف والمخزون والحركات والمستندات والموارد البشرية، ثم يعيد تسلسلات PostgreSQL الخاصة بهذه الجداول.

يحتفظ بـ20 جدول تأسيسي حتى يظل النظام قابلًا للدخول والاستخدام بعد المسح:

- الشركات والفروع.
- دليل الحسابات ومراكز التكلفة.
- السنوات والفترات المالية والعملات وأكواد الضريبة.
- المستخدمون وكلمات المرور والجلسات والأدوار والصلاحيات وربط المستخدمين بالشركات والفروع.
- سجلات التدقيق والنسخ الاحتياطية والقواعد القانونية وقواعد فصل المهام.

## ضوابط الأمان

- متاح فقط في `development` أو `testing` أو `uat`.
- يتطلب `ALLOW_DATA_RESET=true`.
- متاح فقط لدور `SUPER_ADMIN`.
- يعرض معاينة بعدد الصفوف والجداول المتأثرة قبل التنفيذ.
- يتطلب العبارة العربية المطابقة حرفيًا: `تهيئة UAT كاملة - مسح جميع بيانات التشغيل`.
- يتطلب Dry Run ناجحًا ثم رمز تفويض موقّعًا صالحًا لمدة عشر دقائق لنفس لقطة البيانات.
- التنفيذ النهائي يتم داخل معاملة واحدة ويسجل حدثًا واضحًا في سجل التدقيق.
- الإنتاج يرفض التشغيل عند `ALLOW_DATA_RESET=true`، وقالب الإنتاج مضبوط على `false`.

## إصلاح إنشاء المستخدم

- يحول اسم الدخول إلى حروف صغيرة ويستبدل المسافات بنقطة.
- المثال: `Hussein Mahmoud` يصبح `hussein.mahmoud`.
- إذا تُرك اسم الدخول فارغًا، تُنشئ الواجهة الاسم تلقائيًا من الاسم الإنجليزي.
- يتم توليد البريد الافتراضي من اسم الدخول المنظف بدل تكوين بريد غير صالح يحتوي على مسافات.
- ما زالت قواعد منع الرموز غير المسموح بها والتحقق من البريد مطبقة.

## نتائج التحقق

- اختبار R9.1 المتخصص: نجح إنشاء المستخدم، والدخول بالاسم المنظف، ورفض غير مدير النظام، والمعاينة، والـDry Run، والتنفيذ النهائي، والاحتفاظ ببيانات الوصول، وحذف بيانات التشغيل، واستمرار جلسة المدير، وإنشاء مستخدم جديد بعد المسح.
- بوابة الانحدار الكاملة: `70/70` ملف تحقق ناجح خلال `837.8s`.
- بناء الواجهة الإنتاجي: ناجح، `2,042` وحدة.
- تجميع Python وفحص صياغة ملفات التطبيق: ناجح.
- بوابة الإصدار ورأس Alembic: ناجح، `e20500000001`.

## خطوات تشغيل النسخة على Render UAT

1. ارفع محتوى هذه الحزمة إلى المستودع المرتبط بخدمة `corvax-test`.
2. في Render افتح خدمة `corvax-test` ثم **Environment** واضبط:
   - `ENVIRONMENT=uat`
   - `ALLOW_DATA_RESET=true`
   - `APP_VERSION=1.0.0-agreement-completion-rc27.4-r9.1`
3. احفظ المتغيرات ونفذ Deploy لآخر نسخة.
4. سجل الدخول بحساب مدير النظام، ثم افتح **النظام ← تهيئة بيانات UAT**.
5. راجع المعاينة، اكتب عبارة التأكيد كما هي، نفذ Dry Run، ثم نفذ المسح النهائي.
6. بعد ظهور النجاح، ابدأ إدخال بيانات الاختبار شبه الحقيقية.

> تحذير: التنفيذ النهائي عالمي وغير قابل للتراجع من داخل الواجهة. تأكد أن الخدمة هي `corvax-test` وليست خدمة الإنتاج، وخذ نسخة احتياطية خارجية إذا كانت بيانات UAT الحالية مطلوبة لاحقًا.
