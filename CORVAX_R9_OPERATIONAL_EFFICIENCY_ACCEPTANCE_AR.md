# تقرير قبول CORVAX RC27.4 · R9 — الكفاءة التشغيلية والرقابة

تاريخ التحقق: 9 أغسطس 2026  
خط الأساس: CORVAX RC27.4 · R8  
رأس الهجرات: `e20500000001`

## النتيجة التنفيذية

تم تنفيذ R9 كحزمة مترابطة بين المشتريات والمالية والمخزون والمستودعات والجودة وتقنية المعلومات والمراجعة الداخلية والامتثال. نجحت بوابة الانحدار الكاملة في `69/69` اختبارًا خلال `760.9` ثانية، ونجح بناء الواجهة في `2042` وحدة.

## الوظائف المنفذة

1. مركز دورة الشراء من PR وRFQ والمقارنة والترسية إلى PO وGRN وفاتورة المورد وتخصيص السداد، مع المسؤول الحالي ومدة التوقف والمخاطر وفتح المستند الأصلي.
2. ملف مورد رقابي يتضمن السجل والتواصل وشروط السداد والتقييم وآخر أسعار فعلية ومؤشرات آخر 3 و5 عمليات لكل صنف.
3. تغيير IBAN بمسار pending/approve، تشفير القيمة، وإلزام اعتماد مستخدم مستقل مع أثر تدقيق.
4. منع فاتورة المورد المكررة في الواجهة وفي API الحسابات الدائنة، مع قفل معاملات PostgreSQL لنفس هوية المورد/رقم الفاتورة.
5. استلام مخزون Mobile-first بالباركود/QR، مع المقبول والمرفوض والتشغيلة والإنتاج والانتهاء والموقع ودليل الفحص.
6. المقبول فقط يرفع المخزون وGRNI وكمية الاستلام؛ المرفوض لا يدخل المخزون أو المطابقة الثلاثية.
7. تنبيهات فعلية لتأخر PO وتجاوز الاستلام والحد الأدنى وقرب الانتهاء وبطء الحركة، إضافة إلى التنبيهات الرقابية للمنصة.
8. لوحة صحة النظام: التطبيق وقاعدة البيانات والهجرة والأخطاء والنسخ والاستعادة، دون كشف الأسرار.
9. استيراد Excel مرحلي: staging ثم validation ثم approval مستقل؛ لا يوجد ترحيل مباشر عند الرفع، والأرصدة الافتتاحية غير المتوازنة تُرفض.
10. تجهيز ZATCA Sandbox: readiness وUUID وSHA-256 وQR metadata وسجل أدلة الاختبار، مع منع ادعاء الربط الإنتاجي.
11. صلاحيات R9 مستقلة وعزل شركة وفصل مهام وسجل تدقيق مترابط.
12. الإبقاء على تصنيف الأصناف وNRV داخل المخزون والمستودعات كما اعتمد في R8.

## أدلة التحقق

- بوابة الإصدار: `5/5` ناجحة.
- بوابة النظام: `69/69` ناجحة.
- بناء TypeScript/Vite: `2042` وحدة ناجحة.
- ترقية قاعدة جديدة من أول هجرة إلى `e20500000001`: ناجحة.
- اختبار تجاوز منع الفاتورة المكررة عبر API: مرفوض برمز `409`.
- اختبار باركود خاطئ: مرفوض برمز `422`.
- اختبار الاستلام المقبول/المرفوض وسجل الجودة والتنبيهات: ناجح.
- اختبار عزل الشركات، maker-checker، الاستيراد المرحلي وZATCA Sandbox: ناجح.

## حدود الاعتماد الحالية

هذه النسخة مؤهلة لـ Staging/Pilot وUAT. لا يجوز وصفها بأنها معتمدة للإنتاج المالي النهائي قبل إغلاق البنود الخارجية التالية:

1. ربط ZATCA الفعلي وإكمال Onboarding وبيانات المنشأة والشهادات والاختبارات الرسمية.
2. اختبار اختراق مستقل وإغلاق نتائجه.
3. Parallel Run ومطابقة النتائج مع السجلات الفعلية ثم توقيع CFO وIT والعمليات والموارد البشرية والمراجعة الداخلية.
4. حفظ bytes للمرفقات والصور في Object Storage المعتمد؛ R9 الحالية تحفظ metadata وSHA-256 وobject key لدليل الاستلام ولا تدعي تخزين الملف نفسه داخل الجدول.
5. تشغيل اختبار PostgreSQL حي ونسخ احتياطي/استعادة على بيئة Render الفعلية قبل اعتماد الإنتاج.

## تعليمات النشر

1. استخدام PostgreSQL وبيئة الإنتاج المعتمدة دون Demo seed أو schema auto-create.
2. تنفيذ `alembic upgrade head` والتحقق أن `/health/ready` يعرض `e20500000001`.
3. نشر ملفات الواجهة المبنية المضمنة في `backend/static`.
4. إعادة تحميل المتصفح تحميلًا قسريًا بعد النشر.
5. تنفيذ UAT بالأدوار المستقلة وتوثيق نتائج الدورة من الطلب إلى السداد.

