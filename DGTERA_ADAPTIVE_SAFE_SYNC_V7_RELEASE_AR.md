# إصدار DGTERA V7 — التقسيم الآمن والمطابقة الصارمة

> **ملغى للنشر:** أثبت أحدث تقرير DGTERA أن الرقم `12,128` كان مطابقًا داخل
> الموصل والمرآة فقط، وليس لتقرير المصدر الحقيقي (`4,362.00`). استُبدلت
> نافذة التاريخ ومعرّف الإثبات في V8؛ لا تعتمد شهادات V7.

التاريخ: 13 أغسطس 2026

## ما أثبتته الصورة الأخيرة

- يوم 13-08-2026 ظهر في CORVAX بصافي `10,547`، وضريبة `1,581`، وإجمالي `12,128`، وعدد `288` طلبًا.
- شريط المطابقة الأخضر أثبت أن إجمالي DGTERA وإجمالي CORVAX كلاهما `12,128` والفرق `0` في لحظة الالتقاط.
- قيم يوم التشغيل الجاري قابلة للزيادة طبيعيًا مع وصول طلبات جديدة؛ الاعتماد يكون على تطابق المصدر وCORVAX لنفس لحظة وشهادة المزامنة، وليس على بقاء رقم اليوم ثابتًا.
- رسالة الخطأ الحمراء لم تكن فرقًا مقبولًا؛ كانت رفضًا آمنًا لنافذة كبيرة أعادت سجلات أكثر من حد القراءة الكامل، ولذلك لم تُنشأ شهادة يوم 12 وظهر `missing proof`.

## الإصلاح في V7

- عند تجاوز نتيجة نافذة متعددة الأيام حد الأمان، يقسم CORVAX الفترة تلقائيًا إلى نصفين ثم يعيد التقسيم حتى تُقرأ كل نافذة كاملة.
- كل نافذة فرعية تمر بمطابقة مستقلة: معرفات الطلبات، الرؤوس، الفروع، العملاء، الأصناف، السطور، المدفوعات، الحالات، الكمية، الصافي، الضريبة، الإجمالي، المدفوع، المرتجعات، الخصومات وبصمات المصدر.
- لا تُجمع النوافذ إلا إذا كانت كل الشهادات ناجحة وكل الفروق `0.00`.
- لا يُرفع حد السجلات ولا يُقبل أول عدد من السجلات فقط؛ لا يوجد اقتطاع صامت.
- إذا تجاوز يوم أعمال واحد الحد ولا يمكن تقسيمه زمنيًا أكثر، تفشل المزامنة صراحةً وتظل أرقامه محجوبة بدل اعتماد يوم ناقص.
- لا يتقدم وقت آخر مزامنة ناجحة إلا بعد نجاح جميع النوافذ المطلوبة.

## قواعد الأرقام المعتمدة

- المصدر المالي: سطور مبيعات DGTERA/Odoo فقط.
- صافي المبيعات: `price_subtotal`.
- إجمالي المبيعات شامل الضريبة: `price_subtotal_incl`.
- الضريبة: الإجمالي ناقص الصافي.
- رأس الطلب محفوظ للمراجعة ولا يستبدل قيمة سطور تقرير مبيعات الفروع.
- الحالات المشمولة مثل تقرير «مع أوامر غير مغلقة»: `draft`, `paid`, `done`, `invoiced`، مع استبعاد `cancel`.
- اليوم التجاري حسب توقيت `Asia/Riyadh` من `00:00:00` إلى `23:59:59`.
- شركة المطاعم والشركة القابضة تقرآن المرآة نفسها؛ لا يوجد اتصال ثانٍ أو تكرار للإيراد.
- الاستكمال التاريخي يبدأ من `01-01-2025`، والفترة غير المكتملة تظهر `—` إلى أن تحصل على إثبات حي.
- التحديث الآلي كل دقيقتين.

## الاختبارات المنفذة

- محاكاة تجاوز نافذة أربعة أيام لحد DGTERA ثم تقسيمها إلى أربعة أيام كاملة.
- التحقق من مجموع `1,152` طلبًا وإجمالي مصدر ومحلي `48,512.00` وفرق `0.00` بعد الدمج.
- محاكاة يوم واحد يتجاوز الحد والتأكد من رفضه وعدم تحديث `last_sync_at`.
- اختبار حدود يوم الرياض، الطلبات المفتوحة، مصدر السطور، التكرار الآمن، حذف الطلبات الملغاة أو المنقولة، وإلغاء المعاملة عند أي فرق.
- اختبار تطابق القابضة والمطاعم من اتصال وسجل واحد.
- نجاح `53/53` بوابة تحقق آلية، ونجاح بناء TypeScript/Vite الإنتاجي.

## معيار القبول بعد الرفع

لا تعتمد أي فترة إلا عند اجتماع الآتي:

1. ظهور شريط المطابقة الأخضر.
2. المصدر = CORVAX.
3. الفرق = `0.00`.
4. عدد الطلبات والسطور والمدفوعات والكمية متساوٍ.
5. وجود بصمة التحقق وشهادة كل يوم داخل الفترة.
6. عدم وجود `missing proof` أو رسالة حد سجلات حمراء.

بعد الرفع انتظر دورة الاستكمال، ثم افحص 12-08-2026 وفترة من عام 2025. ظهور الشرطة `—` قبل اكتمال الشهادة سلوك آمن، أما ظهور رقم بلا شهادة فمرفوض.
