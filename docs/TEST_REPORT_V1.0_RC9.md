# CORVAX v1.0 RC9 — Test Report

## النتيجة الرئيسية

`CORVAX v1.0 RC9 final consolidation and finance completion: ALL VERIFICATIONS PASSED`

## سيناريو RC9 المنفذ فعليًا

- تشغيل قاعدة مستقلة وترقيتها إلى Migration head `e17000000001`.
- إنشاء مستخدمين منفصلين للإعداد والمراجعة والاعتماد.
- إنشاء مجموعة توحيد وأعضاء وبيانات دفتر أستاذ مرحلة.
- إنشاء وتشغيل ترجمة عملية أجنبية معتمدة.
- إنشاء ميزان مراجعة موحد على مستوى الحساب والتحقق من:
  - تساوي المدين والدائن.
  - تطبيق أوراق التوحيد المعتمدة.
  - تسجيل عدد الأوراق المعلقة.
  - إنشاء بصمة SHA-256 بطول 64 خانة.
  - منع المُعد من المراجعة.
  - منع المراجع من الاعتماد النهائي.
  - منع الاعتماد عند وجود أوراق معلقة.
  - الإقفال بالحالة `APPROVED_LOCKED` بعد استيفاء الضوابط.
- إنشاء قياس مقابل محتمل والتحقق من منع إعادة قياس عنصر حقوق الملكية.
- تنفيذ إعادة قياس التزام لاحقة وإنشاء قيد مرحّل بعد الاعتماد.
- تنفيذ التخلص من عملية أجنبية والتحقق من إنشاء ورقة توحيد CTA معتمدة.
- التحقق من Dashboard المالي، Health، Readiness، الإصدار وMigration head.

## اختبارات الانحدار

تم تشغيل مجموعات الاختبار التالية ونجحت:

- v0.10، v0.12، v0.14، v0.16، v0.18، v0.20.
- v0.22، v0.24، v0.26، v0.28، v0.30، v0.32.
- RC1، RC2، RC4، RC5، RC6، RC7، RC8، RC9.

تغطي المجموعات المالية والدفاتر الفرعية والمخزون والتصنيع الأساسي والأصول والرواتب وPOS والأمن وHR والعملات والتوحيد وIFRS 9 والصيانة والمقدمات والاستحقاقات والمعاملات بين الشركات والإقفالات والحوكمة والجودة وسلامة الغذاء والصلاحيات والتقارير المالية المتقدمة.

## البناء والترحيل

- `python -m compileall`: ناجح.
- اختبارات RC9 واختبارات الانحدار المذكورة: ناجحة.
- TypeScript + Vite production build: ناجح.
- Vite حوّل **1,780 module**.
- ملف JavaScript الإنتاجي: **373.22 kB** قبل الضغط و**105.91 kB** بعد gzip.
- ملف CSS الإنتاجي: **46.85 kB** قبل الضغط و**9.97 kB** بعد gzip.
- `npm audit`: **0 vulnerabilities** وقت بناء هذه الحزمة.
- Fresh Alembic migration حتى `e17000000001`: ناجح.
- عدد الجداول في Fresh Migration: **157**.
- ترقية نسخة RC8 نظيفة إلى RC9: ناجحة.
- الحفاظ أثناء الترقية على 4 شركات، مستخدم، سجل المراجعة، و244 حسابًا سابقًا: ناجح.
- الحسابات بعد الترقية: **268**، وهي زيادة متوقعة قدرها 24 حسابًا.
- ملفات الواجهة الإنتاجية نُسخت إلى `backend/static`.

## ما لم يُختبر داخل هذه الحزمة

- إقفال موازٍ على بيانات الشركة الحقيقية ومقارنة النتائج بنظام قائم أو ملفات مراجعة معتمدة.
- IAS 29.
- بيئة ضريبية متعددة الاختصاصات ومواقف ضريبية غير مؤكدة.
- تقييم مستقل للمقابل المحتمل والقيم العادلة.
- أحمال متزامنة كبيرة أو اختبار اختراق مستقل.
- PostgreSQL على بنية إنتاج حقيقية واختبار استعادة وفشل خدمة.
- اعتماد مراجع خارجي أو مستشار ضريبي.
