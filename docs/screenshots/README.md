# Screenshot evidence status

The images in this directory (`01_login_rc3.png`, `02_company_selector_rc3.png`, and `03_executive_dashboard_rc3.png`) are historical RC3 evidence only.

They must not be represented as RC27 or RC27.2 screenshots. A fresh browser-based visual audit is still required for the current release. The automated Chromium capture attempt in the build container was blocked by the container browser runtime and produced no new screenshots.
